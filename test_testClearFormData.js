// テスト用に使用してください
// 最終的には削除
/*
function testClearFormData() {
  // テスト用のLINE IDを指定
  const testLineId = 'U6471460801f5027cfef392c876127cd1'; // ここにテスト用のLINE IDを入力してください

  // clearFormData関数を呼び出して結果を取得
  const result = clearFormData(testLineId);

  // 結果をログに出力
  Logger.log('Test Result: ' + JSON.stringify(result));

  // 結果をコンソールに出力
  console.log('Test Result: ', result);

  // 結果のメッセージを表示
  if (result.success) {
    Logger.log('Success Message: ' + result.message);
  } else {
    Logger.log('Error Message: ' + result.message);
  }
}

// テスト関数を実行
function runTest() {
  testClearFormData();
}
*/